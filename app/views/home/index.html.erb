<!-- Header Animation -->
<%= render 'header' %>

<!-- Icons / Features Grid -->
<%= render 'features' %>

<!-- Showcases -->
<%= render 'showcase' %>

<!-- Previous Bitcamps -->
<%= render 'previous_bitcamp' %>

<!-- FAQs -->
<%= render 'faqs' %>

<!-- Testimonials -->
<%= render 'testimonials' %>

<!-- Sponsors -->
<%= render 'sponsors' %>

